<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grupodeseguridadreyes.reyes">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegistroPunto"/>

        <activity android:name=".MarcarControl" />
        <activity android:name=".VerificarSupervisor" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SOS" />
        <activity android:name=".RegistroGuardia" />
        <activity android:name=".RegistrarEmpresa" />
        <activity android:name=".Novedades" />
        <activity android:name=".MenuGuardia" />
        <activity android:name=".Historial" />
        <activity android:name=".Enviar" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>